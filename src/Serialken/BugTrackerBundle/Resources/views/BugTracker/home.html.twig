{# src\Serialken\BugTrackerBundle\Resources\views\BugTracker #}

{% extends "SerialkenBugTrackerBundle::layout.html.twig" %}

{% block title %}
   Home - {{ parent() }}
{% endblock %}

{% block body %}
    <p >
     Bienvenue sur la page d'accueil !!!!!!
     <br />
     
   </p>
   <div>  
   {% if app.user %}
        {% if is_granted('ROLE_USER') %}   
            
            {{ 'Welcome'|trans }} {{ app.user.username }} -  {{ 'Last login'|trans }}: {{ app.user.lastlogin|date('d/m/Y') }} - <a href="{{ url('fos_user_security_logout') }}" class="btn btn-mini" >
                                                                   <i class="glyphicon glyphicon-off"></i> {{ 'Logout' }}
                                                                    
                                                            </a>
        {% endif %}
    {% else %}
        <p >
     Veuillez vous connectez pour accéder a plus d'options > <a href="{{ url('fos_user_security_login') }}" > {{ 'Login' }} </a>
        </p>
    
    {% endif %}
   </div>
{% endblock %}